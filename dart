Cannot specify arguments before a command.

Usage: build_runner <command> [arguments]

Global options:
-h, --help    Print this usage information.

Available commands:
  build   Builds the current package.
  clean   Deletes the build cache. The next build will be a full build.
  run     Builds the current package, executes a script.
  serve   Continuously builds and serves the current package.
  test    Builds the current package then runs tests.
  watch   Continuously builds the current package.

Run "build_runner help <command>" for more information about a command.
